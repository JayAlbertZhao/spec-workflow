# Project workspaces — each user's own projects live alongside this repo
# but are not part of it
cc_academic/
testcases/
projects/

# Local migrated session archives may contain private transcripts or secrets.
docs/archive/

# Working state in any project subdir (kept for safety)
*/.agents/
*/experiments/
*/ref/

# Local settings
*/.claude/settings.local.json
.claude/settings.local.json

# Editor / OS
.DS_Store
*.swp
__pycache__/
*.pyc

# Secrets — never commit
.env
**/.env
